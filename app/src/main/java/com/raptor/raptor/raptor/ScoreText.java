package com.raptor.raptor.raptor;

import android.graphics.Canvas;
import android.graphics.Paint;

import java.util.ArrayList;

class ScoreText {
    private Paint paint;
    ScoreText() {
        paint = new Paint();
        paint.setAntiAlias(true);
    }
    void draw(Canvas canvas, ArrayList<Float> sc) {

    }
}
